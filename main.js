// JAVASCRIPT

// Variables
const NUM_ROWS = 10;
const NUM_COLS = 10;


// Create an array to represent grid
let grid = createGridArray();

// Create divs to model the grid array
createDivGrid(grid);

